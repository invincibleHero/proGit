1. 当执行git add 命令之后，又重新修改 相关的文件，此时，如果，只是想提交之前add 的文件到git仓库，而不提交后来修改的内容，该怎么办？？？
2. 可不可删除 ./git/object 目录下的一些镜像文件 （同时删除 git的工作目录和 暂存区中的内容） ？？？
3. 我们知道，所有保存到git的暂存区中的文件都有一个快照，我们如何快速找到一个文件的所有快照，(按照修改顺序)？？
4. 之前的测试，发现，工作区域删除文件后，通过 git add  文件名后， 并没有删除，暂存区中的镜像文件，我们应该如何将这些文件找出来？？？
