 为什么有的文件通过执行 git add 命令后，保存到 暂存区后，在object目录下对应的文件可读，有的却是乱码？？？不可读    因为使用notepad++ 创建的文件的格式是ANSI ,而不是 utf8 格式的。

2. 为什么一个本地仓库可以对应多个远程仓库，为什么要这样做？？ (猜测可能是分支)
3. 如何查看以跟踪文件清单？？？

4. 有时候，我们提交完了才发现漏掉了几个文件没有加，或提交信息写错了。想要撤销刚才的提交操作，我们可以使用 -amend 选项重新提交。
	是不是说，如果我们修改了3个文件，但是只是提交了1个文件，是不是，之后，再使用amend选项进行重新提交的时候，会将那些之前没有提交的内容也全部提交？？
	而且好像也没有必要要撤销吧，？？这个和撤销应该没有关系吧？？  没有检测出来。。
5.  使用 git commit 提交单个文件的时候，会得到一个 类似于 [master 30b2969]  的字符串，我们知道其中 30是object目录下的一个文件夹，后面的b2969 是提交文件在./git/object 目录下的镜像文件，
但是，提交多个文件的时候，也是得到一个类似的字符串，这是，我们应该如何得到此次提交时，多个文件在 object中的镜像快照？？？？？


6. git 可以添加多个git 仓库，但是，当存在多个远程仓库的时候，如何，确定将代码提交到哪一个仓库？？ 或者是同时向多个仓库提交？？

7. 当远程分支，存在多个分支的时候，如何控制要跟踪哪些分支，不要跟踪哪些分支？？

8. 部署标签有什么用？？在什么情况下使用？？
	git tag -s v0.2 -m "my signed 0.2 tag "

9. 应该可以根据标签，使用maven来打包摸个标签处的代码，该怎么弄弄？？？

10.如何单独上传一个文件到github上面

	


